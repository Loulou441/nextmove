<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  URL de base de l'API FastAPI.
	  Priorité de résolution dans NextMoveAPI.swift :
	    1. Variable d'environnement NEXTMOVE_API_URL (schéma Xcode)
	    2. Cette clé Info.plist
	    3. Défaut compilé : localhost:8000 (simulateur) / 192.168.1.175:8000 (device)

	  DEV  (simulateur)  : laisser vide, le défaut http://localhost:8000 s'applique.
	  DEV  (iPhone réel) : mettre http://192.168.1.175:8000 (IP LAN du Mac).
	  PROD (Railway)     : mettre l'URL HTTPS Railway, ex. https://nextmove-api.up.railway.app
	-->
	<key>NEXTMOVE_API_URL</key>
	<string>https://nextmove-production-9996.up.railway.app</string>

	<!--
	  App Transport Security.
	  En production (HTTPS) aucune exception n'est nécessaire.
	  Pour le dev sur device physique (HTTP LAN), ajouter temporairement
	  l'exception ci-dessous et retirer avant de soumettre à l'App Store :

	  <key>NSAppTransportSecurity</key>
	  <dict>
	    <key>NSAllowsLocalNetworking</key><true/>
	    <key>NSExceptionDomains</key>
	    <dict>
	      <key>192.168.1.175</key>
	      <dict>
	        <key>NSExceptionAllowsInsecureHTTPLoads</key><true/>
	      </dict>
	    </dict>
	  </dict>
	-->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
</dict>
</plist>
