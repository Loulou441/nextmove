# Dépendances du backend FastAPI pour le déploiement Railway.
# Version allégée : sans torch / ultralytics / streamlit / opencv, inutiles à
# l'API (l'app mobile fait sa CV en local et pousse le résultat via
# POST /matches/sync). Nixpacks détecte ce fichier et installe avec pip.
alembic==1.19.1
bcrypt==5.0.0
chromadb
email-validator==2.3.0
fastapi==0.121.0
groq==1.5.0
httpx==0.28.1
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic_core==2.46.4
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
sentence-transformers==3.0.1
SQLAlchemy==2.0.52
starlette
supabase==2.31.0
uvicorn==0.52.3
