[phases.setup]
nixPkgs = ["python311", "libgl1", "libglib2.0"]

[phases.install]
cmds = ["pip install -r backend/requirements.railway.txt"]

[start]
cmd = "python -m uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT"
